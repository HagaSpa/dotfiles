#!/bin/bash
#MISE description="Install Claude Code via native installer"
set -euo pipefail

if command -v claude >/dev/null 2>&1; then
  echo "Claude Code is already installed"
  exit 0
fi
curl -fsSL https://claude.ai/install.sh | bash
